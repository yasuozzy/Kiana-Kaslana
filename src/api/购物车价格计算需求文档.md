# 购物车价格计算API需求文档

## 📋 文档说明
- **文档版本**: v1.0
- **创建日期**: 2025-10-05
- **前端负责人**: [你的名字]
- **适用项目**: 足球商店购物车系统

---

## 🎯 需求背景

目前购物车的价格计算在**前端**进行，存在以下问题：
1. ❌ **安全风险**：前端价格可被用户篡改
2. ❌ **数据不一致**：价格变动时前端可能显示旧价格
3. ❌ **业务复杂度**：优惠券、折扣等计算逻辑应该在后端

**解决方案**：所有价格计算由后端完成，前端只负责展示。

---

## 📊 现状分析

### 当前前端的价格计算逻辑
```javascript
// 前端 Vuex 代码（需要废弃）
getters: {
  // 计算购物车总价
  cartTotalPrice: (state) => {
    return state.cartItemsList.reduce((total, item) => {
      return total + (item.price * item.quantity)  // 前端自己算
    }, 0)
  },
  
  // 计算商品总数量
  cartItemsCount: (state) => {
    return state.cartItemsList.reduce((total, item) => {
      return total + item.quantity
    }, 0)
  }
}
```

### 前端显示的数据格式
```vue
<!-- 订单摘要显示 -->
<div class="order-details">
  <h4>订单摘要</h4>
  <div class="price-row">
    <span>商品小计</span>
    <span>¥{{ cartTotalPrice }}</span>         <!-- 所有商品的总价 -->
  </div>
  <div class="price-row">
    <span>商品数量</span>
    <span>{{ cartItemsCount }} 件</span>       <!-- 总共多少件商品 -->
  </div>
  <div class="price-row total">
    <span>购物车合计</span>
    <span>¥{{ cartTotalPrice }}</span>         <!-- 合计价格 -->
  </div>
  
  <!-- 如果使用了优惠券 -->
  <div v-if="appliedCoupon" class="price-row coupon-row">
    <span>优惠券 ({{ appliedCoupon }})</span>
    <span>-¥{{ discountAmount.toFixed(2) }}</span>  <!-- 优惠金额 -->
  </div>
  
  <div class="price-row total">
    <span>购物车优惠后价格</span>
    <span>¥{{ finalPrice.toFixed(2) }}</span>   <!-- 最终支付价格 -->
  </div>
</div>
```

---

## 🔧 需要后端提供的接口改进

### 1️⃣ 获取购物车接口（需要增强）

#### 当前接口
```
GET /api/cart/:userId
```

#### 当前返回格式
```json
{
  "success": true,
  "data": {
    "userId": 1,
    "items": [
      {
        "id": 1759595590733,
        "productId": 1,
        "productName": "切尔西 Nike Dri-Fit Adv 客场比赛球衣 2025-26",
        "price": 122,
        "quantity": 1,
        "size": "M",
        "subtotal": 122        // ⚠️ 目前有这个字段，但前端没用上
      }
    ],
    "totalAmount": 122,        // ⚠️ 目前有这个字段，但前端没用上
    "totalItems": 1,           // ⚠️ 目前有这个字段，但前端没用上
    "updatedAt": "2025-10-04T16:00:00.000Z"
  }
}
```

#### ⭐ 需要增强的返回格式
```json
{
  "success": true,
  "data": {
    "userId": 1,
    "items": [
      {
        "id": 1759595590733,              // 购物车项ID（删除、更新时用）
        "productId": 1,                   // 商品ID
        "productName": "切尔西 Nike Dri-Fit Adv 客场比赛球衣 2025-26",
        "productImage": "/images/xxx.jpg", // 商品图片
        "price": 122,                     // 商品单价（后端实时价格）
        "originalPrice": 150,             // 商品原价（用于显示划线价格）
        "quantity": 3,                    // 数量
        "size": "M",                      // 尺码
        "brand": "Nike",                  // 品牌
        "subtotal": 366,                  // ⭐ 小计 = price × quantity（后端计算）
        "discount": 0,                    // 该商品的优惠金额（如果有单品折扣）
        "stock": 10,                      // 库存数量（前端可以限制用户输入）
        "isAvailable": true               // 商品是否可购买
      },
      {
        "id": 1759595590734,
        "productId": 2,
        "productName": "AC米兰 PUMA 主场球衣 2025-26",
        "productImage": "/images/yyy.jpg",
        "price": 130,
        "originalPrice": 160,
        "quantity": 2,
        "size": "L",
        "brand": "PUMA",
        "subtotal": 260,                  // 130 × 2 = 260（后端计算）
        "discount": 20,                   // 这件商品有20元优惠
        "stock": 5,
        "isAvailable": true
      }
    ],
    
    // ⭐⭐⭐ 重点：购物车汇总信息（全部由后端计算）
    "summary": {
      "totalItems": 5,                    // 总商品数量 = 3 + 2
      "totalAmount": 626,                 // 商品总价 = 366 + 260（后端计算）
      "totalDiscount": 20,                // 总优惠金额（后端计算）
      "shippingFee": 0,                   // 运费（后端计算，满99免运费等逻辑）
      "finalAmount": 606                  // 最终支付金额 = 626 - 20 + 0（后端计算）
    },
    
    "updatedAt": "2025-10-05T16:00:00.000Z"
  },
  "timestamp": 1728151234567
}
```

#### 字段说明

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `items[].id` | Number | ✅ | 购物车项ID，删除、更新时需要 |
| `items[].subtotal` | Number | ✅ | **后端计算**的小计（单价×数量） |
| `items[].discount` | Number | ✅ | 该商品的优惠金额 |
| `items[].stock` | Number | ✅ | 库存数量，用于前端限制 |
| `items[].isAvailable` | Boolean | ✅ | 商品是否可购买（下架商品返回false） |
| `summary.totalItems` | Number | ✅ | **后端计算**的总商品数量 |
| `summary.totalAmount` | Number | ✅ | **后端计算**的商品总价 |
| `summary.totalDiscount` | Number | ✅ | **后端计算**的总优惠金额 |
| `summary.finalAmount` | Number | ✅ | **后端计算**的最终支付金额 |

---

### 2️⃣ 更新购物车商品数量接口（需要改进）

#### 当前接口
```
PUT /api/cart/update
```

#### 当前请求参数
```json
{
  "userId": 1,
  "itemId": 1759595590733,
  "quantity": 4
}
```

#### 当前返回格式
```json
{
  "success": true,
  "message": "商品数量已更新"
}
```

#### ⭐ 需要改进的返回格式

**方案1：返回完整购物车数据（推荐）**
```json
{
  "success": true,
  "message": "商品数量已更新",
  "data": {
    // 返回完整的购物车数据（格式同"获取购物车接口"）
    "userId": 1,
    "items": [ /* ... */ ],
    "summary": {
      "totalItems": 6,           // 更新后的总数量
      "totalAmount": 748,        // 更新后的总价（后端重新计算）
      "totalDiscount": 20,
      "finalAmount": 728
    },
    "updatedAt": "2025-10-05T16:05:00.000Z"
  },
  "timestamp": 1728151534567
}
```

**方案2：只返回更新的商品信息（备选）**
```json
{
  "success": true,
  "message": "商品数量已更新",
  "data": {
    "updatedItem": {
      "id": 1759595590733,
      "quantity": 4,              // 更新后的数量
      "subtotal": 488,            // 后端重新计算的小计 (122 × 4)
      "price": 122                // 当前单价
    },
    "summary": {
      "totalItems": 6,
      "totalAmount": 748,         // 后端重新计算的总价
      "totalDiscount": 20,
      "finalAmount": 728
    }
  }
}
```

**推荐用方案1**，原因：
- 前端不需要再发一次请求获取购物车
- 保证数据完全一致
- 代码更简单

---

### 3️⃣ 删除购物车商品接口（需要改进）

#### 当前接口
```
DELETE /api/cart/remove/:itemId
```

#### 当前返回格式
```json
{
  "success": true,
  "message": "商品已删除"
}
```

#### ⭐ 需要改进的返回格式
```json
{
  "success": true,
  "message": "商品已删除",
  "data": {
    // 返回删除后的完整购物车数据
    "userId": 1,
    "items": [ /* 剩余商品 */ ],
    "summary": {
      "totalItems": 2,           // 删除后的总数量
      "totalAmount": 260,        // 删除后的总价（后端重新计算）
      "totalDiscount": 20,
      "finalAmount": 240
    },
    "updatedAt": "2025-10-05T16:10:00.000Z"
  }
}
```

---

### 4️⃣ 添加商品到购物车接口（需要改进）

#### 当前接口
```
POST /api/cart/add
```

#### 当前请求参数
```json
{
  "productId": 1,
  "quantity": 1,
  "selectedSize": "M"
}
```

#### 当前返回格式
```json
{
  "success": true,
  "message": "商品已添加到购物车",
  "data": { /* ... */ }
}
```

#### ⭐ 需要改进的返回格式
```json
{
  "success": true,
  "message": "商品已添加到购物车",
  "data": {
    // 返回添加后的完整购物车数据
    "userId": 1,
    "items": [ /* 包括新添加的商品 */ ],
    "summary": {
      "totalItems": 3,           // 添加后的总数量
      "totalAmount": 388,        // 添加后的总价（后端计算）
      "totalDiscount": 0,
      "finalAmount": 388
    },
    "updatedAt": "2025-10-05T16:15:00.000Z"
  }
}
```

---

## 🔥 重点强调

### 为什么所有接口都要返回完整数据？

1. **数据一致性**：前端数据和后端完全同步
2. **减少请求**：不需要"更新数量"后再"获取购物车"
3. **实时价格**：每次操作都能获取最新价格
4. **用户体验**：操作后立即看到最新的总价

### 后端需要计算的内容

| 计算项 | 说明 | 示例 |
|--------|------|------|
| `subtotal` | 每个商品的小计 | price × quantity = 122 × 3 = 366 |
| `totalItems` | 购物车商品总数量 | 3 + 2 = 5 |
| `totalAmount` | 所有商品的总价 | 366 + 260 = 626 |
| `totalDiscount` | 总优惠金额 | 商品折扣 + 优惠券 = 20 |
| `finalAmount` | 最终支付金额 | 626 - 20 = 606 |

### 后端需要处理的业务逻辑

1. ✅ **价格获取**：从数据库获取商品当前价格（不信任前端传的价格）
2. ✅ **库存检查**：数量不能超过库存
3. ✅ **商品状态**：下架商品标记为不可购买
4. ✅ **价格计算**：所有金额计算在后端完成
5. ✅ **优惠计算**：促销、折扣、优惠券等在后端计算

---

## 💻 前端使用示例

### 示例1：用户改变商品数量
```javascript
// 前端代码
const handleQuantityChange = async (itemIndex, newQuantity) => {
  try {
    // 1. 调用后端API更新数量
    const response = await axios.put('/api/cart/update', {
      userId: getUserId(),
      itemId: cartItemId,
      quantity: newQuantity
    })
    
    // 2. 后端返回完整的购物车数据（包含重新计算的价格）
    const cartData = response.data.data
    
    // 3. 前端直接用后端的数据更新界面
    updateCartUI(cartData)  // 更新商品列表
    
    // 显示后端计算的价格
    console.log('总商品数:', cartData.summary.totalItems)
    console.log('商品总价:', cartData.summary.totalAmount)
    console.log('总优惠:', cartData.summary.totalDiscount)
    console.log('最终支付:', cartData.summary.finalAmount)
    
  } catch (error) {
    console.error('更新失败:', error)
  }
}
```

### 示例2：显示购物车总价
```vue
<template>
  <div class="order-details">
    <h4>订单摘要</h4>
    
    <!-- 直接使用后端返回的数据 -->
    <div class="price-row">
      <span>商品小计</span>
      <span>¥{{ cartData.summary.totalAmount }}</span>
    </div>
    
    <div class="price-row">
      <span>商品数量</span>
      <span>{{ cartData.summary.totalItems }} 件</span>
    </div>
    
    <div class="price-row" v-if="cartData.summary.totalDiscount > 0">
      <span>优惠金额</span>
      <span>-¥{{ cartData.summary.totalDiscount }}</span>
    </div>
    
    <div class="price-row total">
      <span>应付金额</span>
      <span>¥{{ cartData.summary.finalAmount }}</span>
    </div>
  </div>
</template>
```

---

## 🚨 错误处理

### 库存不足的情况
```json
{
  "success": false,
  "message": "库存不足",
  "code": "INSUFFICIENT_STOCK",
  "data": {
    "requestedQuantity": 10,    // 用户想买10件
    "availableStock": 3,         // 实际只剩3件
    "productName": "切尔西球衣"
  }
}
```

### 商品已下架的情况
```json
{
  "success": false,
  "message": "商品已下架",
  "code": "PRODUCT_UNAVAILABLE",
  "data": {
    "productId": 1,
    "productName": "切尔西球衣"
  }
}
```

---

## 📝 接口改造优先级

### P0（最高优先级）- 必须改
1. ✅ 获取购物车接口：返回 `summary` 字段
2. ✅ 更新数量接口：返回完整购物车数据

### P1（高优先级）- 建议改
3. ✅ 删除商品接口：返回完整购物车数据
4. ✅ 添加商品接口：返回完整购物车数据

### P2（中优先级）- 可以后续优化
5. ⚠️ 库存检查
6. ⚠️ 商品状态验证
7. ⚠️ 优惠券计算

---

## 🤝 前后端协作

### 后端需要做的
1. 修改4个接口的返回格式（增加 `summary` 字段）
2. 在后端计算所有价格（单价×数量、总价、优惠等）
3. 每次操作后返回完整购物车数据

### 前端需要做的
1. 调整数据结构，使用后端返回的 `summary` 数据
2. 删除前端的价格计算逻辑
3. 所有价格显示直接用后端返回的数据

### 测试要点
1. ✅ 改变数量后，价格立即更新
2. ✅ 删除商品后，总价正确减少
3. ✅ 添加商品后，总价正确增加
4. ✅ 刷新页面后，价格和后端一致

---

## 📞 联系方式

如有疑问，请联系前端负责人：[你的联系方式]

---

## 📄 附录

### 完整的购物车数据结构（供参考）
```json
{
  "success": true,
  "data": {
    "userId": 1,
    "items": [
      {
        "id": 1759595590733,
        "productId": 1,
        "productName": "切尔西 Nike Dri-Fit Adv 客场比赛球衣 2025-26",
        "productImage": "//images.football-fanatics.com/chelsea-nike.jpg",
        "price": 122,
        "originalPrice": 150,
        "quantity": 3,
        "size": "M",
        "brand": "Nike",
        "color": "蓝色",
        "subtotal": 366,
        "discount": 0,
        "stock": 10,
        "isAvailable": true
      }
    ],
    "summary": {
      "totalItems": 3,
      "totalAmount": 366,
      "totalDiscount": 0,
      "shippingFee": 0,
      "finalAmount": 366
    },
    "updatedAt": "2025-10-05T16:00:00.000Z"
  },
  "timestamp": 1728151234567
}
```

---

**文档结束** 🎉

