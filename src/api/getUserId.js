// 🔑 工具函数：获取当前登录用户的ID
const getUserId = () => {
   // 第一步：尝试从localStorage本地获取用户信息
   const userStr =localStorage.getItem('currentUser')

     // 第二步：检查纸条是否存在
     if(userStr){
       // 如果纸条存在，解析JSON字符串变成对象 把字符串格式的用户信息转换成对象格式
       const user = JSON.parse(userStr)

          // 第三步：检查用户对象里是否有ID user 用户信息对象存在吗 user.userId 用户信息里有userId这个字段吗
          if(user && user.userId){ //如果都存在，则返回用户ID
            console.log('✅ 找到用户ID:', user.userId)
            return user.userId
          }
             // 如果有用户信息但没有userId字段，可能是id字
             if(user && user.id){
              console.log('✅ 找到用户ID:', user.id)
              return user.id
             }
     }
       // 第四步：如果找不到，提示警告，并返回默认值1
       console.warn('⚠️ 未找到登录用户信息，使用默认用户ID: 1')
       return 1  // 兜底方案，返回默认值
}
export default getUserId  // 导出函数 让其他文件可以调用
